<FrameLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:foreground="@drawable/btn_image_selector"
	android:padding="1dp" >   

    <ImageView
        android:id="@+id/gallery_item_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/app_name"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_photo_default"
        android:background="@drawable/common_photo_bg"/>
    
	 <ImageView
            android:id="@+id/gallery_item_image_selected"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right|bottom"
            android:padding="4dp"
            android:src="@drawable/ic_toggle_check"
            android:contentDescription="@string/app_name"
            android:gravity="center"
            android:visibility="gone"
             />
</FrameLayout>
